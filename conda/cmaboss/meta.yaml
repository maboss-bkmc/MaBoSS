{% set name = "cMaBoSS" %}
{% set version = "1.0.0b12" %}

package:
  name: '{{ name|lower }}'
  version: '{{ version }}'

source:
  path: ../..


build:
  number: 0

requirements:
  build:
    - {{ compiler('cxx') }}
    - flex
    - bison
    - make
    - python {{ python }}
    - numpy {{ numpy }}
    - setuptools
    - libsbml-plus-packages-namespace
    - libxml2
    
  host: 
    - python {{ python }}
    - numpy {{ numpy }}
    - setuptools
    - libsbml-plus-packages-namespace
    - libxml2
  
  run: 
    - python {{ python }}
    - numpy {{ numpy }}
    - setuptools
    - libsbml-plus-packages-namespace
    - libxml2

about:
  home:  https://maboss.curie.fr

