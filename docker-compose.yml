#docker-compose.yml
version: '3.3'
services:

  maboss-server:
    build:
      context: ./
      dockerfile: docker/Dockerfile

    image: maboss-server
    container_name: maboss-server
    ports:
      - "7777:7777"
    restart: unless-stopped

